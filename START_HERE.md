# Open the application

The repository root is not the website. The browser directory page appears when a generic file server is pointed at this folder.

Run the frontend from the repository root:

```powershell
npm run dev
```

Then open `http://127.0.0.1:3000`.

Use the Student / Lecturer / Admin selector at the top of the sidebar to switch portals.

